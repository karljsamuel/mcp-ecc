services:
  mcp-ecc:
    build: .
    container_name: mcp-email-calendar-contacts
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MCP_ENCRYPTION_KEY=${MCP_ENCRYPTION_KEY}
    volumes:
      - ./data:/data
    command: ["start", "--sse"]
