{
  "name": "mcp-ecc",
  "version": "0.1.0",
  "description": "MCP Server for Email, Calendar, and Contacts with Headless OAuth Support",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/storage/*",
    "packages/providers/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "format": "prettier --write \"packages/**/*.{ts,json,md}\"",
    "cli": "node packages/cli/dist/bin.js",
    "api": "node packages/management-api/dist/index.js",
    "workers:deploy": "cd packages/workers-entry && wrangler deploy"
  },
  "devDependencies": {
    "turbo": "^1.13.0",
    "prettier": "^3.2.0",
    "husky": "^9.0.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.8.0"
}